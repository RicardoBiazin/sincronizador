# Basico: FTP/FTPS (stdlib), SFTP e WebDAV.
paramiko>=3.0
requests>=2.28
pyinstaller>=6.0

# cryptography: vem via paramiko e cifra os segredos. Piso >=50 fecha advisories
# de versoes antigas (OpenSSL no wheel, oracle PKCS#7, DoS na cadeia de cert).
cryptography>=50
